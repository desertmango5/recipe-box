doctype html
html
  head
    title= `${title} | ${h.siteName}`
    link(rel='stylesheet' href='/dist/style.css')
    link(rel="shortcut icon" type="image/png" href="/images/icons/recipe-box.png")
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    block header
      header.header
        .content
          h1 Recipe Box

    block content